# ClawScale Environment Configuration
# Copy this file to .env and fill in values

# ─── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://clawscale:clawscale@localhost:5432/clawscale

# ─── Auth ──────────────────────────────────────────────────────────────────────
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=7d


# ─── OpenClaw Bridge ───────────────────────────────────────────────────────────
# Path to the OpenClaw binary. When set, ClawScale starts one isolated
# OpenClaw gateway per tenantId/channelId/endUserId/backendId.
OPENCLAW_BIN=openclaw
# ClawScale spawns per-tenant OpenClaw gateway instances; their ports are
# assigned dynamically starting from this base.
OPENCLAW_PORT_BASE=19000
# Directory where isolated OpenClaw state/ and workspace/ data is stored
OPENCLAW_DATA_DIR=./data/tenants
# Optional command template. Placeholders: {profile}, {port}, {stateDir}, {workspaceDir}
# OPENCLAW_ARGS="--profile {profile} gateway --port {port}"

# ─── Docker OpenClaw bind mount ────────────────────────────────────────────────
# Use with: docker compose -f docker-compose.yml -f docker-compose.openclaw.yml up -d --build
# Host-side OpenClaw install directory. It must contain both the openclaw entry
# file and its dist/ build output; mounting only /usr/bin/openclaw is not enough.
OPENCLAW_HOST_DIR=/path/to/openclaw
# Container-side mounted OpenClaw install directory and entry path.
OPENCLAW_CONTAINER_DIR=/opt/openclaw
OPENCLAW_BIN_CONTAINER=/opt/openclaw/openclaw
# Host/container data paths for isolated OpenClaw state and workspaces.
OPENCLAW_DATA_DIR_HOST=./data/openclaw
OPENCLAW_DATA_DIR_CONTAINER=/app/data/openclaw
